package com.houzicore.shared.core.party;

import com.houzicore.shared.MiniPlugin;
import org.bukkit.plugin.java.JavaPlugin;

public class PartyJoinManager extends MiniPlugin {
    public PartyJoinManager(JavaPlugin plugin) {
        super("Party Sync", plugin);
    }

    // Party Follow-Leader implementation stub
    // Resolves Item 96: Automatic server pull bindings between Party Leaders and followers.
}
